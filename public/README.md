# Public Assets

This folder contains static files served directly by the deployed web application.

The `_redirects` file is used by static hosts to route API traffic and support client-side React navigation.
