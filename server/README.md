# Local Backend Services

This folder contains Node/TypeScript services used for local forensic analysis and Smart Money scanner workflows.

The Vite frontend proxies selected `/api/*` routes to these services during development.
